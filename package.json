{
  "name": "mini-sync-obsidian",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "ci": "pnpm lint && pnpm -r run build && pnpm test",
    "dev:sync-remote": "tsx packages/core-application/src/dev/sync-remote-folder.ts",
    "dev:sync-drive": "tsx packages/core-application/src/dev/sync-drive.ts",
    "dev:watch": "tsx packages/core-application/src/dev/watch-vault.ts",
    "build": "pnpm -r run build",
    "build:core": "pnpm --filter @mini-sync/core-domain run build && pnpm --filter @mini-sync/core-application run build",
    "build:desktop": "pnpm --filter @mini-sync/desktop run build",
    "build:all": "pnpm run build:core && pnpm run build:desktop",
    "check:workspace": "node scripts/check-workspace-names.mjs",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --write .",
    "format:check": "prettier . --check .",
    "dev": "pnpm -r run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.27.0",
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/node": "^25.0.3",
    "@vitest/coverage-v8": "^4.0.18",
    "electron": "^29.4.6",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.7.4",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.51.0",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "chokidar": "^5.0.0"
  }
}
